D.Barker 10Mar2012
===========================
Overview of changes for 2.4.2
===========================
- fix buffer overflow problem is 4 or more ADC cards are defined by
a single model. It was found that the 4th ADC data was written to
the DAC data space.

Two lines in controller.c were changed

-double dHistory[96][40];
-double dDacHistory[96][40];

+double dHistory[(MAX_ADC_MODULES * 32)][MAX_HISTRY];
+double dDacHistory[(MAX_DAC_MODULES * 16)][MAX_HISTRY];

