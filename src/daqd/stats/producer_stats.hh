#ifndef __PRODUCER_STATS_PLUS_PLUS_H__
#define __PRODUCER_STATS_PLUS_PLUS_H__

#include <stats.hh>

class producer_stats : public stats
{
public:
    ~producer_stats( );
};

producer_stats::~producer_stats( ){};

#endif
